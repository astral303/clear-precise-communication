# Second person only in product text

Changelogs, README, UI copy, and error strings: `you`. Titles, commits, MR descriptions, issues, comments: `the user`. Same facts, different readers.

`You` in backticks is a product string, not the wrong person.
