# Relay findings in reader terms

A relayed review item is your artifact. Open with what the user sees and
when, then the mechanism, then the fix. At most one symbol per sentence, after
the effect. Do not forward a reviewer's function-first shorthand.

Where a finding says "before X, now Y", say which is the bug and whether it
predates the branch. Verify a scope-deciding claim in the code first.
